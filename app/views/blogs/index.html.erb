<div class="container">
  <div class="row">
    <div class="col-xs-12">
      <h1>瀬戸内の雲のように</h1>
      <h3 class="text text-muted">クラウド時代に取り残されそうな私の技術メモ</h3>
    </div>
  </div>
  <hr>
  <div class="row">
    <!-- プロフィール -->
    <div class="col-xs-12 col-md-5 col-lg-4">
      <%= render 'meta' %>
    </div>
    
    <!-- ブログ一覧 -->
    <div class="col-xs-12 col-md-7 col-lg-8">
      <% @blogs.each do |blog| %>
      
        <!-- ブログを1件ずつ row に入れる-->
        <div class="row row-blog">
          
          <!-- サムネイルの表示 -->
          <div class="col-xs-2 col-thumbnail">
            <%= link_to blog_path(blog.id) do %>
              <% if blog.picture != nil %>
                <%= image_tag(blog.picture.image.thumb) %>
              <% else %>
                <!-- no imageの画像を表示 -->
                <%= image_tag(@noimage.image.thumb) %>
              <% end %>
            <% end %>
          </div>
        
          <div class="col-xs-6">
            <!-- 件名 -->
            <div class="row">
              <h3><%= link_to blog.title, blog_path(blog.id) %></h3>
            </div>
            
            <!-- 投稿日付 -->
            <div class="row">
              <p class="text text-muted"><%= blog.created_at.strftime('%Y-%m-%d %H:%M:%S') %></p>
            </div>
            
            <!-- 本文 -->
            <div class="row">
              <%= blog.content.first(30) %>...
            </div>
          </div>
          
          <!-- 続きを読む => いらないか？
          <div class="col-xs-2">
            <%#= link_to '続きを読む', blog_path(blog.id) , :class => 'btn btn-default' %>
        
            <% if current_user != nil %> 
              <!-- 管理者はここから編集に行けるようにする 
              <%= link_to '編集', edit_blog_path(blog.id) , :class => 'btn btn-primary' %>
            <% end %>
          </div>
          -->
        </div><!-- blogを1件ずつrowに入れる のend %>
      <% end %><!-- @blogs.each の end -->
    </div>
  </div>
</div>